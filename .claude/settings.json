{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath' | { read -r f; case \"$f\" in /home/chong/.local/share/chezmoi/*) target=$(chezmoi target-path \"$f\" 2>/dev/null) && chezmoi apply \"$target\" ;; esac; } 2>/dev/null || true",
            "timeout": 10,
            "statusMessage": "Applying chezmoi..."
          }
        ]
      }
    ]
  }
}
